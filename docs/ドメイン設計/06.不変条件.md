- [不変条件（Invariant）](#不変条件invariant)


# 不変条件（Invariant）

絶対に破ってはいけないルール。

* 振替関係は必ず 1 つの支払元を持つ
* 振替関係は必ず 1 つの支払先を持つ
* 同じ「支払元 + 支払先」の振替関係は 1 つだけ
* 1 つの振替関係の支払元と支払先に同じものは指定できない
* 振替関係を変更する場合は、選択した支払先に対して、支払元のみを変更できるようにする

---
